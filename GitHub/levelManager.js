var levelDirector = me.levelDirector.extend({
	loadLevel : function(){
		//Use: me.levelDirector.loadLevel(level);
	}
	nextLevel : function(){
		//Call this when the player finishes the level
	}
	reloadLevel : function(){
		//Call this when the player dies 
	} 
	
})
